[project]
name = "agentic-forex-desk"
version = "0.1.0"
description = "Private MCP-powered forex scanner, charting, paper trading, and controlled execution backend."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
